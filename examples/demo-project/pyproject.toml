[project]
name = "demo-fastapi"
version = "0.1.0"
description = "Small repository for the PiX coding-agent demo."
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115", "httpx>=0.27", "pytest>=8.0"]
